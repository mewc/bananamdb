language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
# deploy:
#   provider: firebase
#   skip_cleanup: true
#   token: <not set>
# on:
#   branch: master
